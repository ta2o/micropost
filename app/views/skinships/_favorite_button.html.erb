<% unless current_user == user %>
  <% if current_user.favorite?(user) %>
    <%= form_for(current_user.skinships.find_by(like_id: user.id), html: { method: :delete }) do |f| %>
      <%= hidden_field_tag :like_id, user.id %>
      <%= f.submit 'Unfavorite', class: 'btn btn-danger btn-block' %>
    <% end %>
  <% else %>
    <%= form_for(current_user.skinships.build) do |f| %>
      <%= hidden_field_tag :like_id, user.id %>
      <%= f.submit 'Favorite', class: 'btn btn-primary btn-block' %>
    <% end %>
  <% end %>
<% end %>